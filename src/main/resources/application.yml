spring:
  application:
    name: gh-simple-spring
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: optional:configserver:http://localhost:9090
server:
  port: 9000

simple:
  greeting:
    message: ${greeting.message}